# QA-026 v0.3.0-rc.5 Release Candidate Acceptance

## Result

- Date: 2026-08-27
- Release source: tag `v0.3.0-rc.5`, commit
  `e97afcdf5823c481a24aab1152ca88bdfeae9131`
- Result: **PASS**
- Published prerelease:
  <https://github.com/chenrgSix/AgentRoom/releases/tag/v0.3.0-rc.5>

This acceptance closes release task `QA-026`. The candidate packages the
central HTTPS port 9443 and LAN IP compatibility work from `OPS-006`, the
Server image policy-asset repair from `OPS-007`, and the Windows Desktop
enrollment request-body repair from `BRG-041`.

Prerelease publication does not claim stable admission, signed or notarized
packages, automatic updates, Windows login startup, physical Windows enrollment
acceptance, the real macOS login-restart gate `BRG-013`, or the
two-physical-machine gate `QA-002`.

## Exact-Source Continuous Integration

Main CI run
[33059955167](https://github.com/chenrgSix/AgentRoom/actions/runs/33059955167)
completed successfully on the exact tagged commit. Repository, Go, Desktop
macOS, and Desktop Windows jobs passed locked dependency installation, schema
and fixture validation, production builds, workspace tests, full Go tests and
vet, deterministic cross-process E2E, documentation lint, patch checks,
Compose validation, native desktop builds, and Windows package smoke checks.

The focused `BRG-041` regression simulates a Wails/WebView2 enrollment request
with a non-empty JSON body and a reported content length of zero. It proves the
exact URL `https://192.168.1.132:9443` and pinned SHA-256 configuration reach
the enrollment client rather than becoming an empty `serverUrl`.

## Release Workflow

Release Bridge run
[33060171424](https://github.com/chenrgSix/AgentRoom/actions/runs/33060171424)
validated the exact tag and empty Draft Release, tested the tagged Bridge,
built five portable CLI archives, two native macOS Desktop archives, the
Windows Desktop portable archive, and the Windows installer. It generated
checksums and added `LICENSE`, `NOTICE`, `COMMERCIAL-LICENSE.md`, and
`TRADEMARKS.md`. The committed verifier accepted all 14 assets before upload
and again after downloading the Draft assets.

The first workflow attempt stopped before any build or upload after the
pre-existing large-WebSocket test timed out while waiting for client cleanup.
The exact source had already passed main CI; the focused test then passed 20
consecutive local runs. The empty Draft was confirmed before rerunning the
workflow, and the second attempt completed every build, upload, and verification
job successfully.

The Windows installer from this release has SHA-256
`d8bb92f97308ecbacfce8c753810d8535070bf17abde1f0d6d5b36f586709f6c`.
The Windows Desktop portable ZIP has SHA-256
`251c8c636d867bd35373b495edb45866543cb19a0e081ceb303a60cb1e325aa4`.

## Independent Public Asset Verification

A fresh directory at
`/tmp/agentroom-v030rc5-public-assets.czlnmj` downloaded the public prerelease
after publication. The first GitHub CLI download ended with a transport `EOF`;
a retry-capable public download resumed the incomplete assets. The tagged
verifier then reported `Verified 14 release assets for v0.3.0-rc.5`. It
rechecked all nine binary archives and installers, checksums, safe archive
layout, launchers, architectures, embedded versions, GUI and installer
metadata, and the four license files.

The public Release remains a prerelease, while `v0.2.0` remains the public
Latest stable release.

## Remaining Gates

- A person still needs to install this candidate on a physical Windows machine
  and exercise the repaired pinned-certificate enrollment flow, SmartScreen,
  wizard, WebView2, tray behavior, and a live Codex launch.
- `BRG-013` remains active until a real macOS logout/login or restart proves
  exactly one desktop instance without credentials in the LaunchAgent.
- `QA-002` remains ready until two physical machines produce reviewed online
  and offline/reconnect Codex Runs.
- Packages remain unsigned, unnotarized, and manual-update-only.
- `v0.2.0` remains the stable Latest release while this version is evaluated as
  a prerelease candidate.
