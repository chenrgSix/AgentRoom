# QA-030 Packaged Physical Rehearsal Partial Evidence

Date: 2026-08-28

Status: `PARTIAL` — this is a sanitized read-only observation of a real
packaged Central and a different physical Windows Bridge. It is not a generated
schema-v3 `PASS` record and does not complete `QA-030`, `QA-002`, or `QA-028`.

## Exact package boundary

- The physical macOS arm64 Central reports release `v0.4.0-qa030.1`, source
  commit `9af9edae36ed5fce2cf5b3b2ef06a7cfd91b8d4b`, lifecycle step `ready`,
  `direct_https`, `private_scoped_ca`, and trust epoch 1.
- The consumed Device pairing session records a Windows amd64 client reporting
  Bridge `0.4.0-qa030.1`, the same private-scoped installation identity and
  trust epoch, and scoped-private-trust capability support.
- The independently downloaded public Windows installer still matches SHA-256
  `f10d1e3f60d87750e91b6bf91e93fdbc0145db940ebe0b8f5979eaff5a5745f8`.
  The machine-B operator later confirmed that the locally used installer
  produced the same digest.

No private origin, local path, credential, certificate material, CA digest,
prompt, or Runtime output is recorded here.

## Live Server facts

At `2026-08-28T06:29:42Z`, read-only SQLite and internal metrics reported:

- one consumed private-scoped pairing session;
- one active, authenticated, online Device;
- one enabled, ready managed Agent with a path-free Workspace projection;
- three completed Runs, each with one accepted Delivery, a contiguous terminal
  trace, exactly one reply event, and exactly one Agent Message;
- `agentroom_up 1`, `agentroom_bridge_connections 1`,
  `agentroom_managed_agents 1`, zero queued Runs, zero pending Deliveries, zero
  delivery retries, and three completed Runs.

These are real cross-host product facts. They prove substantially more than the
earlier manual-CA reachability diagnostic, but they do not prove the human and
offline/reconnect boundaries below.

## Same-Device recovery observation

Later read-only observations of the same physical installation proved:

- the one active Device published two enabled managed Agents, both with
  path-free Workspace projections, without creating or pairing another Device;
- the machine-B operator explicitly ran the saved Codex Runtime self-test and
  confirmed `RUNTIME_PROBE_OK`;
- after Bridge stopped, the Server reported zero authenticated Bridge
  connections and both Agents offline;
- a unique nonce Run for the same Agent used by the earlier online Runs stayed
  `queued`, with a persisted `pending` Delivery whose send count remained zero;
- the installed Bridge restarted with the same Device identity, accepted that
  exact Delivery, and advanced the same Run through contiguous sequences for
  `delivered`, `working`, output, reply, and `completed`;
- the final reply matched the requested nonce exactly, and the trace contained
  exactly one reply event and one Agent-authored Message; and
- the drained system reported one authenticated Bridge, two ready managed
  Agents, zero queued Runs, and four completed Runs.

An earlier nonce was accidentally queued for the second Agent and canceled
before any send. Its immutable Delivery correctly remains unaccepted with send
count zero, but the packaged Server's metrics incorrectly count that terminal
historical row as actionable pending work. `RUN-013` fixes the metric semantics
at commit `28f997e`; that untagged local fix is not evidence for the packaged
candidate and the old metric output cannot be rewritten into a PASS record.

The later `v0.4.0-qa030.2` Central upgrade rehearsal did not reach the Windows
Device gate. A zero-byte recovery source passed the released mode-only
`doctor`, then caused `secret-init` to reject the target after backup, image
build and container replacement. The exact retained authority restored the old
Central without changing its installation identity or private CA. `OPS-010`
adds the missing content preflight, but this partial record must wait for a new
exact package; it does not reinterpret the failed `qa030.2` rehearsal as an
upgrade.

## Lifecycle-safe Central upgrade observation

The exact public `v0.4.0-qa030.3` candidate includes `OPS-010`, `RUN-013` and
`CON-015`. Its native release workflow and a separate ordinary public download
both accepted exactly 22 assets. The independently downloaded Windows
current-user installer has SHA-256
`19a1815660dc98f43b1ea61e55e5e5c966521f58b464b60d844db37b60d53222`.

The same physical Central used the public Darwin arm64 archive and separate
internal checksum pin, created a verified SQLite backup, and upgraded from
`v0.4.0-qa030.1` to `v0.4.0-qa030.3`. It retained its installation identity,
private CA and trust epoch, reports schema 48 and `ready`, and passes packaged
HTTPS/WebSocket `doctor` checks. The existing Windows Device reconnected
without another pairing at a newer connection epoch, still reporting Bridge
`0.4.0-qa030.1`; its two managed Agents were both ready.

Current packaged metrics report `agentroom_up 1`, one authenticated Bridge,
two managed Agents, queue depth zero, pending Delivery count zero, and four
completed Runs. The retry total remains a historical counter and is not
substituted for actionable pending work. This advances the Central and drained
metric boundaries but is still partial: the same Device must install the
`qa030.3` Bridge, report that current build independently, and complete the
human-reviewed schema-v3 record.

## Remaining final-record conditions

The strict schema-v3 verifier intentionally rejects the current snapshot until:

1. the same Device installs the exact `v0.4.0-qa030.3` Bridge and records that
   authenticated current version independently of the original pairing;
2. the operator records the safe local Codex version string;
3. human review confirms the installed deep link opened, the verification
   phrase matched, no CA was installed into the Windows trust store, and no
   application TLS-verification bypass, Server Token copy, or Device credential
   copy occurred; and
4. the complete facts are captured together by the database-bound schema-v3
   verifier and reviewed while the two physical machines remain available.

Until all four conditions are present in one reviewed schema-v3 record, this
partial observation must not be relabeled as `PASS`.
