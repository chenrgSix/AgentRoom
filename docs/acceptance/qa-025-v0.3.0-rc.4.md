# QA-025 v0.3.0-rc.4 Release Candidate Acceptance

## Result

- Date: 2026-08-27
- Release source: tag `v0.3.0-rc.4`, commit
  `93cb0a1abc7e7e7cf3d79582e86c5db0c83e53bf`
- Result: **PASS**
- Published prerelease:
  <https://github.com/chenrgSix/AgentRoom/releases/tag/v0.3.0-rc.4>

This acceptance closes release task `QA-025`. The candidate packages the
completed AgentRoom Community License 1.0 adoption from `GOV-011` and the
Windows Runtime launcher compatibility repair from `BRG-040` while retaining
every existing CLI, Desktop, and installer artifact.

Prerelease publication does not claim stable admission, signed or notarized
packages, automatic updates, Windows login startup, human Windows UI or live
Codex command-shim acceptance, the real macOS login-restart gate `BRG-013`, or
the two-physical-machine gate `QA-002`.

## Exact-Source Continuous Integration

Main CI run
[33053742206](https://github.com/chenrgSix/AgentRoom/actions/runs/33053742206)
completed successfully on the exact tagged commit. Repository, Go, Desktop
macOS, and Desktop Windows jobs passed locked dependency installation, seven
schema and 57 fixture validation, production builds, 129 Server tests, 42 Web
tests, four contract tests plus Go contract checks, 23 Bridge UI tests, full Go
tests and vet, deterministic cross-process E2E, documentation lint, patch
checks, Compose validation, release-script checks, and native desktop builds.

The native Windows job executed the new Windows-only regression that passes an
npm-style `codex.cmd` without Unix execute bits through PATH discovery and the
authenticated Runtime preflight endpoint. It then built the installer,
installed it without elevation at the default LocalAppData path, performed an
in-place upgrade, uninstalled it, and preserved owner configuration.

## Release Workflow

Release Bridge run
[33053982720](https://github.com/chenrgSix/AgentRoom/actions/runs/33053982720)
validated the exact tag and empty Draft Release, tested the tagged Bridge,
built five portable CLI archives, two native macOS Desktop archives, the
Windows Desktop portable archive, and the Windows installer. It generated
checksums and added `LICENSE`, `NOTICE`, `COMMERCIAL-LICENSE.md`, and
`TRADEMARKS.md`. The committed verifier accepted all 14 assets before upload
and again after downloading the Draft assets.

The Windows installer from this release has SHA-256
`42c221ed79e4618d7f217b205dadcbcdd033ba15de3867c9e30b4176ebe19a25`.
The Windows Desktop portable ZIP has SHA-256
`451400964f4e577e4d71e853f144ed0c77a269317540598c9841afde3d05590c`.

## Independent Public Asset Verification

A fresh directory at
`/private/tmp/agentroom-v030rc4-public-assets.dBh65E` downloaded the public
prerelease after publication. The tagged verifier ran directly on macOS and
reported `Verified 14 release assets for v0.3.0-rc.4`. It rechecked all nine
binary archives and installers, checksums, safe archive layout, launchers,
architectures, embedded versions, GUI and installer metadata, and the four
license files.

The public Release remains a prerelease, while `v0.2.0` remains the public
Latest stable release. The license shipped in `v0.3.0-rc.4` is the AgentRoom
Community License 1.0; earlier published versions retain the terms shipped
with those versions.

## Local Preflight Note

The first local E2E and workspace-test attempts reached the macOS sandbox's
protected user Go build cache and failed before affected Go subprocesses could
build. Complete reruns with task-scoped writable `GOCACHE` and `GOMODCACHE`
passed. The exact tagged GitHub-hosted CI and Release jobs did not reproduce the
host permission issue.

## Remaining Gates

- A person still needs to exercise the installer on a physical Windows machine
  and report SmartScreen, wizard, WebView2, tray, application behavior, and a
  live Codex command-shim launch.
- `BRG-013` remains active until a real macOS logout/login or restart proves
  exactly one desktop instance without credentials in the LaunchAgent.
- `QA-002` remains ready until two physical machines produce reviewed online
  and offline/reconnect Codex Runs.
- Packages remain unsigned, unnotarized, and manual-update-only.
- `v0.2.0` remains the stable Latest release while this version is evaluated as
  a prerelease candidate.
