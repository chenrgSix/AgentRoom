# QA-060 ConveneWire v0.5.0 Stable Release Admission

Status: goal and admission gates frozen by the Owner on 2026-09-04 before
stable tag creation, Draft creation, packaging or publication. Delivery state
lives only in `docs/TASKS.md`.

## Goal

Publish one new immutable `v0.5.0` stable Latest release containing the
completed Governed Software-Team Execution Core and the accepted `QA-059`
stability repair. Stable admission is evidence-based and has no implicit
elapsed-time requirement. Freezing this record fixes the rules; it does not
claim that the rules have passed or authorize publication.

The published `v0.5.0-rc.6` prerelease is the current immutable evaluation
evidence. Its tag and 12 assets must not be moved, replaced, deleted or
relabeled. The earlier public `v0.5.0-rc.3`, `v0.5.0-rc.4` and
`v0.5.0-rc.5` candidates remain immutable prior evidence. Unpublished
`v0.5.0-rc.1` and `v0.5.0-rc.2` remain immutable zero-asset failed-closed
records. Stable Latest remains `v0.4.2` until every gate in this record passes.

## Stable Product Boundary

The stable claim is limited to source-available, self-hosted use by trusted
small Teams:

- humans govern exact Plans, approvals, evidence adoption and repository
  integration operations;
- Central governs Plan, scheduling, bounded authorization,
  Evidence/Proof/Adoption and operation-receipt facts;
- repository paths, Git remotes, Git/SSH credentials, fetch/pull/push,
  worktrees and Git command execution remain Client/Bridge and machine-Owner
  authority;
- the Remote Evidence implementation remains an optional,
  credential-free-by-default extension and is not a Core or stable-admission
  dependency; and
- packages remain unsigned and unnotarized, updates remain manual, and the
  release is not a public multi-tenant or general-production admission.

`BRG-013`, `WEB-050` and `QA-038` remain separate active work. Stable
publication does not close them and does not claim macOS login-restart,
exhaustive Hosted-Agent production-browser states, a paid/live provider,
multiple physical execution machines, signed distribution, automatic updates
or a production deployment. A future claim in any of those areas requires its
own accepted task and current evidence.

## Pre-Tag Admission Gates

Before creating a stable tag or Draft, all of the following must be retained:

1. Review the public RC, repository issues and known release findings. No
   unresolved P0 or P1 correctness, security, recovery, upgrade, data-loss or
   packaging finding may remain in the admitted scope.
2. Select one full commit on `main` after the frozen admission record. The
   worktree must be clean, local `main` must equal `origin/main`, and every
   runtime or packaging change after `v0.5.0-rc.5` must have a named task,
   regression evidence and an updated boundary in this record.
3. That exact commit must pass the complete main CI: Repository, Go, native
   macOS Desktop and native Windows Desktop jobs. A retry is evidence only when
   the original failure is diagnosed and any repair is committed separately.
4. A disposable installation must take and verify an online `v0.4.2` Central
   backup, upgrade the exact candidate through migration `0086`, preserve the
   installation, Device and Agent identities, and report the candidate release
   version and source commit from the running service.
5. Exact candidate packages must complete one Owner-reviewed product smoke on
   supported hardware: Discussion decision, Plan proposal/revision, exact human
   approval, governed Bridge Run, independent verification, exact-target
   integration, adopted downstream input and owner-confirmed worktree cleanup.
   The smoke may use two real Bridge owners on one physical host; it does not
   claim multiple physical machines or a live Remote Provider.
6. The rollback boundary must be exercised in a disposable target by restoring
   the verified pre-upgrade backup with matching `v0.4.2` binaries. Old binaries
   must never be started against the migrated database. Source repositories,
   unrelated worktrees and Owner credentials must remain unchanged.
7. Before and after the pre-tag gates, both inspected temporary locations must
   show no new `agentroom-*`, `agent-room-*`, `convenewire-*` or
   `convene-wire-*` directories, and the owned test-run root must be physically
   absent after cleanup.

## RC Use Observation — 2026-09-04

The Owner-requested RC observation ran at `2026-09-04T10:22:22+08:00`. It
closes pre-tag gate 1 for the evidence visible at that time; it does not select
the final stable source or satisfy the packaged backup, upgrade, restore and
product-smoke gates.

### Post-RC Candidate Changes — BRG-072 And BRG-073

After the RC observation, BRG-072 added an explicit Bridge/Desktop assistant
for preparing a Windows browser to trust an already authenticated private-CA
Central without manually transferring a certificate file. BRG-073 corrected
the product surface to explain that another Windows, macOS or Linux browser may
need this trust, added a fingerprint-verified current-user macOS command and
retained an honest distribution/browser-specific Linux boundary. The accepted
[BRG-072 record](brg-072-private-browser-trust-assistant-goal.md) retains full
Bridge, race, vet, macOS Desktop, Windows cross-build, embedded UI, docs,
physical cleanup and real responsive-browser evidence; the accepted
[BRG-073 record](brg-073-cross-platform-browser-trust-goal.md) retains the
cross-platform correction and the same authority boundary.

This changes candidate Bridge/Desktop packages and must be included in the
exact stable-source CI and package/smoke gates. It does not change Central,
Team, Device, repository or client-entry authority, does not enable a
certificate-warning bypass, and does not satisfy native Windows trust-store
acceptance. Both the `v0.5.0-rc.3` and `v0.5.0-rc.4` tags and assets remain
immutable.

### Accepted Transition Candidate — v0.5.0-rc.4

`QA-061` published and accepted `v0.5.0-rc.4` from exact source
`f59ef4d991ea7e175d6c3c3d2ce61e56738f6d11`. Exact-source CI 33835256439 and
all 19 protected Release jobs in 33835816394 passed, including native Windows
v0.4.2-to-rc.4 upgrade/state preservation and both clean-daemon Central OCI
architectures. The closed 22-asset set totals `562042643` bytes with outer
`SHA256SUMS` digest
`59bd02533396a26e8d36776d202c7cc046c11eb1a54bff9f72619d36c7847723`;
authenticated Draft and credential-free public downloads both passed the
tagged verifier. Stable Latest remains `v0.4.2`, and exact-source SITE-004
Pages bytes name rc.4 as the current prerelease.

This transition candidate admits BRG-072/BRG-073 packaging into the stable
line but does not satisfy stable pre-tag gates 4 through 6: disposable packaged
backup/upgrade identity, Owner-reviewed product smoke and explicit v0.4.2
rollback remain required before selecting a stable source.

### Prior Accepted Transition Candidate — v0.5.0-rc.5

`QA-062` published and accepted `v0.5.0-rc.5` from exact source
`f19cde69207db2ac2449271842c255fa6fd451cc`. Exact-source CI 33854180942 and
all 19 protected Release jobs in 33854807205 passed, including native Windows
`v0.4.2`-to-rc.5 upgrade/state preservation and both clean-daemon Central OCI
architectures. The closed 22-asset set totals `562155758` bytes with outer
`SHA256SUMS` digest
`03b04890b044e219cc3b75d36211e90da85f9c151a40e95352e8d495b1ccf3a2`;
authenticated Draft and credential-free public downloads both passed the
tagged verifier. Exact-source SITE-005 Pages bytes name rc.5 as the current
prerelease, and stable Latest remains `v0.4.2`.

This candidate adds the explicit CA-free trusted-LAN browser mode, the neutral
Graphite Central visual system and settings-only advanced private-HTTPS trust
entry. It supersedes rc.4 for evaluation without changing the completed Core
or Client-owned repository boundary. It still does not satisfy stable pre-tag
gates 4 through 6: disposable packaged backup/upgrade identity, Owner-reviewed
product smoke and explicit v0.4.2 rollback remain required before selecting a
stable source.

### Post-RC Distribution Change — ADR-0041

After rc.5, the Owner approved ADR-0041. The next candidate was required to
remove Intel
macOS, standalone macOS/Windows CLI and the default embedded Central OCI matrix.
Desktop packages retain the advanced CLI as an internal same-source helper,
Linux retains standalone CLI archives, and Central becomes one verified source
archive built locally through the existing controller. This is a packaging and
installation-boundary change, so rc.5 remains immutable evidence but cannot be
promoted or reused as the stable binary set.

### Current Accepted Streamlined Candidate — v0.5.0-rc.6

`QA-063` published and accepted `v0.5.0-rc.6` from exact source
`6349571870df16e753a4397644a0e4f4eff003ed`. Exact-source CI 33940607248 and
all 10 protected Release jobs in 33940981997 passed without retry, including
native Windows v0.4.2-to-rc.6 upgrade/state preservation, Linux CLI builds,
native Apple-silicon Desktop and the single Central source package.

The closed 12-asset set totals `49106801` bytes with outer `SHA256SUMS` digest
`4efcaac201e20c9ee611119a263bb70bc6f12c5ac34cfbce58b09d12749449c4`.
Authenticated Draft and credential-free public downloads both passed the exact
tagged verifier. A physical Apple-silicon macOS host built and started the
verified Central source package through `convenewirectl`; status, running-image
labels and doctor bound release/source identity, and cleanup removed its exact
owned root. Stable Latest remains `v0.4.2`.

This closes `QA-063` and supplies the required ADR-0041 candidate. It does not
satisfy stable pre-tag gate 5 by itself. The physical record below separately
satisfies the v0.4.2 Central backup/upgrade and matching-old-binary rollback
requirements in gates 4 and 6; the Owner-reviewed governed product smoke still
remains required before selecting a stable source.

### Public Release And Executable

- Public `v0.5.0-rc.3` remained non-draft and prerelease. Annotated tag object
  `72d87761e776e0159d218387a8bb2405d1ed6c41` still peeled to exact commit
  `66ed5d4bf8b68c81d3d8d0a41f9e26b45b34b0e0`.
- The closed release still exposed exactly 22 assets totaling `561993878`
  bytes. Every asset had a GitHub digest, and the newest asset update remained
  `2026-09-03T12:41:38Z`, before publication at
  `2026-09-03T13:20:12Z`. Aggregate download count was 165; that count includes
  project verification traffic and is not asserted to represent external
  users.
- A credential-free download of public `SHA256SUMS` retained SHA-256
  `8c88f36a08c80ce53438fb15e43ab3dad14cb2b8a3bb73913d40a20c83705eac`.
  The public macOS arm64 CLI archive matched its manifest digest
  `b901d9852a217f27ddb5f168a2ebd611bfe80cf258453f5cd4d789703f517a5e`.
- The extracted artifact was an executable arm64 Mach-O. Its supported
  `version` command exited zero and reported `v0.5.0-rc.3`. The Release page
  returned HTTP 200, public `/releases/latest` returned HTTP 200 at `v0.4.2`,
  and the public product site named `v0.5.0-rc.3` as the current prerelease.
- Root-level `convenewire-bridge --help` returned the existing explicit
  `unknown command "--help"` error. The CLI implements `version` and
  command-specific flag sets rather than a root help command; no installation,
  release verifier or documented stable path depends on root `--help`. This is
  retained as a non-blocking usability observation, not hidden or treated as a
  successful help check.

### Feedback, Findings And CI

- GitHub exposed zero open Issues, zero open pull requests and no Issue or pull
  request created after RC publication. There was therefore no repository-
  reported release blocker or attributable external usage report to evaluate.
- `QA-055` retained no open P0/P1 Core finding. The only non-DONE registered
  work besides `QA-060` was `BRG-013`, `WEB-050` and `QA-038`, all explicit
  stable exclusions above. No active task contradicted the frozen stable
  boundary.
- Exact RC CI run `33754552101` and 19-job Release run `33755312963` remained
  completed successfully for commit `66ed5d4`. Post-RC runtime and packaging
  bytes were unchanged: the only paths changed through observation commit
  `fd97440` were README, site copy/tests, task state and acceptance records.
- Main CI run `33828159042` completed Repository, Go, native macOS and native
  Windows jobs successfully for exact observation commit
  `fd97440e65b5c630f2f643d6d5b10b1b60dfe95c`.

### Security Visibility Boundary

GitHub Secret Scanning reported zero open alerts. Dependabot alerts were
disabled and Code Scanning returned `no analysis found`; those services cannot
be cited as zero-finding evidence. A production-dependency `npm audit` attempt
against the configured npm mirror failed because that mirror does not
implement the audit endpoint. A second task-owned attempt against the official
npm registry did not return within the bounded observation window and was
terminated; both isolated caches were physically removed.

This observation therefore means **zero known in-scope P0/P1 release
blockers**, based on the public feedback channels, retained Core audit, current
task register, immutable RC package observation and exact-source CI. It does
not mean zero unknown vulnerabilities or replace a future dedicated security
audit. Unavailable vulnerability services are an explicit evidence limit, not
a passing result.

### Cleanup And Decision

Both anonymous-download roots and both npm-audit roots were removed. Before
and after the observation there were zero matching historical-prefix
directories in `/private/tmp` and the user temporary directory, and `lsof +L1`
reported zero held-deleted matching files.

**Decision at observation time: PASS — no known in-scope RC release blocker.**
The later physical record below closes the packaged backup, upgrade and restore
requirements. Do not create the stable tag or Draft until the remaining product
smoke, final source selection and exact-source CI gates pass.

## Physical v0.4.2 Upgrade And Rollback — 2026-09-05

An Owner-requested physical compatibility run used the public `v0.4.2` macOS
arm64 Central archive and public `v0.5.0-rc.6` source archive on an Apple-
silicon macOS host with Docker Engine `29.4.0` and Compose `5.1.2`. The test ran
from `2026-09-05T05:17:36Z` through cleanup at `2026-09-05T05:26:12Z` in the
single owned root `/private/tmp/convenewire-v042-rc6-upgrade.jBltFY`. Repository
`main` and `origin/main` both resolved to
`0f2e9bec4bfee70297f1d4ba703712cb3f0f37e8` before the evidence-only update.
The existing user Central at `192.168.1.4:9443` used a different Compose project,
database and ports, was never targeted, and was healthy immediately before the
test and after cleanup.

The downloaded asset bytes matched their public outer Release checksums. The
extracted archives then passed their exhaustive internal `SHA256SUMS` checks:

- `v0.4.2` archive SHA-256
  `ab8950bdde2a86252e5d129eef86d3de5d403325c28f8c82a44c8a7716529d8f`,
  internal checksum digest
  `c25fe547ed2fde837913e2385bdb383413e8590e441ed991d0a119393d3d00b3`,
  source `307d6610df532d3c778c293dd4a514e7b895044e`, schema `55`;
- `v0.5.0-rc.6` archive SHA-256
  `ee109693b03eb1a968d4f75afe23f9fa19cfce018ac3a4607b1fc4b416f4632a`,
  internal checksum digest
  `9a0c8d659bbf2879f4683d998b8d771a54dabf767d432ad8602e5382eb0f7163`,
  source `6349571870df16e753a4397644a0e4f4eff003ed`, schema `86`.

### Backup And Upgrade Evidence

The isolated `v0.4.2` installation used Compose project
`cwupgraderc6jbltfy`, loopback ports `42160/42161` and installation identity
`install_9AJd1qNT-mqbpWOAuN7zhhWo`. Owner HTTP operations created one User,
Member, Team, Room and Message; the released fake-Agent test route created the
seeded Device and Agent identities without claiming a physical Bridge. The
verified online backup was mode `0600`, passed SQLite `integrity_check`,
reported schema `55` and had SHA-256
`4fa39915fe64fbea63d1925825a871b772f04659940f0574f9672f1a2e1387da`.

The first exact-target upgrade attempt stopped before target Compose startup
because the test harness sandbox denied Docker Buildx access to its host
activity file. ConveneWire retained the verified automatic backup and durable
upgrade journal. Repeating the same target under ordinary Docker filesystem
authority resumed that journal and succeeded; no different target or backup
was substituted.

After upgrade, controller `status` and `doctor` passed and the running Server
image labels reported release `v0.5.0-rc.6` and source
`6349571870df16e753a4397644a0e4f4eff003ed`. The original browser session was
still authenticated. A second online snapshot passed `integrity_check`,
reported schema `86`, and retained one row in each seeded entity class. The
following identities matched the pre-upgrade record byte for byte:

| Entity | Preserved identity |
| --- | --- |
| Installation | `install_9AJd1qNT-mqbpWOAuN7zhhWo` |
| User | `user_M7o-Cp6JiYKBPWQkriX_cw` |
| Member | `member_UsLFyMj4QRVh7fQUs0HoaA` |
| Team | `team_aVKB9K_lVU4shXd-KJigKg` |
| Room | `room_YFgdz8Ejeur1T7x10j3B8g` |
| Device | `device_plUldyZMohqCizaFPOdnLw` |
| Agent | `agent_BzC7lF9bzUm4zM7DxehV9Q` |
| Message | `msg_LExoXgSCf_UsGE4Wz50lFw` |

### Matching-Binary Rollback Evidence

A second isolated rollback target used project `cwrollbackv042jbltfy` and
ports `42162/42163`. Its Server was stopped before restore. The controller
staged the verified pre-upgrade backup without overwrite as
`/data/convene-wire-rollback-v042.sqlite`; the staged bytes retained the exact
`4fa39915...` digest, passed `integrity_check`, and reported schema `55` before
startup. The generated environment was explicitly changed to that path and the
original mode-`0600` Owner recovery file was installed before starting only the
checksum-pinned `v0.4.2` images. No `v0.4.2` binary was started against the
schema-`86` database.

The restored target passed `v0.4.2` controller `status` and `doctor`; active
Server labels reported source `307d6610df532d3c778c293dd4a514e7b895044e`.
The unchanged Owner recovery value established a new authenticated session,
and the User, Member, Team, Room, Device, Agent and Message identities above all
matched the pre-upgrade record. This closes pre-tag gates 4 and 6 for rc.6.

Both owned Compose projects were uninstalled without purge and the exact test
root was then physically removed. Before and after snapshots retained the same
one pre-existing matching directory in `/private/tmp` and the same 17
pre-existing matching directories in the user temporary root; neither owned
Compose project nor network remained. This run therefore adds no residue. Gate
7 must still be rechecked around the remaining product smoke and final pre-tag
closure.

**Decision at the upgrade handoff: PASS for pre-tag gates 4 and 6.** `QA-060` remains
`ACTIVE`: gate 5, final stable-source selection and exact-source CI still must
pass before a stable tag or Draft is authorized.

## Packaged Governed Product Smoke — 2026-09-05

The Owner requested completion of the missing product-smoke evidence. The
retained `tests/e2e/governed-two-bridge-integration.test.ts` now has an explicit
released-artifact entry point, documented in
[Testing and Observability](../modules/testing-observability.md). It does not
modify Central, Bridge, Web, contracts, migrations or packaging. No runtime or
packaging source changed after rc.6 for this acceptance run.

### Exact Artifacts And Isolation

The public macOS arm64 Desktop ZIP was independently downloaded from the rc.6
Release. Its SHA-256 matched the public asset digest
`96d3d7e554e44915108596c8ea73fd6b30bbfd8b4c7f1a360381b34790b43f83`.
The extracted app's bundled CLI helper reported `v0.5.0-rc.6` and had SHA-256
`499ba9c6f93d633a089aa0b92edd68f792ad1e97b16f33856b29a2fa3feaad06`.
Both native Bridge owners ran that helper, not a locally rebuilt substitute.

Central reused the exact image built from the checksum-verified rc.6 source
package in the upgrade run above:
`sha256:97c76593eec8e418c1012b000fe034fafb9e04ced378fc9d400483ec80c013a5`.
Image labels and the running process's `convenewire_build_info` metric matched
release `v0.5.0-rc.6` and source
`6349571870df16e753a4397644a0e4f4eff003ed`. Four explicit pins are mandatory;
the negative incomplete-pin check failed before Central or Bridge startup and
still reclaimed its owned run root.

Every run used a fresh UUID-named container, loopback-only port and separate
database beneath `/private/tmp/qa060-product.dARLZ3/runs`. The container mounted
only its disposable Central data directory, never either client Git checkout.
Trusted-team setup issued the temporary Owner Cookie; HTTP approvals carried
the exact configured Origin and frozen revision/digest. API calls and owner-
local commands exercised the product's real authority checks. Direct SQL was
read-only evidence inspection, never state advancement.

This is API-driven execution on one Apple-silicon Mac, with two actual native
Bridge processes and a deterministic local Codex app-server fixture. Human-
role actions were performed by the harness under the Owner's test request;
they are not evidence that the Owner personally clicked through the browser
or reviewed each synthetic plan. Caddy/HTTPS, GUI acceptance, live models,
multiple physical machines and deployment of a stable package are not claimed.

### Harness Findings Retained

The first container start correctly rejected the source fixture's `local`
authentication mode on a non-loopback container listener. The harness was
changed to supported trusted-team setup and Cookie authentication; the
product's listener guard was not relaxed.

An initial packaged parallel run rejected an integration receipt with HTTP
409; a diagnostic rerun exited Central with code `135` while native host SQL
assertions were reading its live container database. That observer crossed
Linux/macOS WAL/SHM locking and shared-memory boundaries. The harness now makes
an online backup inside Linux and closes it in DELETE journal mode before
macOS opens the completed snapshot. The original 409 response body was not
retained, so its specific rejected field cannot be retrospectively asserted;
the unsafe observer is removed and both scenarios are rerun without it.

Docker shutdown also exposed a test-lifecycle race: removing the container
before stopping its already-exiting CLI process group could report `kill
EPERM`, including with ordinary host process authority. The harness now stops
the owned CLI first and removes any remaining exact UUID-named container in
`finally`. No signal assertion, product authorization or cleanup requirement
was skipped, and none of these failed attempts is counted as a pass.

### Retained Product Assertions

The controlled loop asserts one completed Discussion with three completed
turns, two immutable Plan revisions, one idempotent human approval, two
compiled nodes and one edge, one completed manual Tech Lead Run, two governed
dispatches and completed Runs, two Results, two passing independent
verifications, one successful integration and one adopted downstream input.
It restarts the actual Central between verification and integration, verifies
exact Git objects/refs and downstream bytes, replays owner-local cleanup and
proves both worktrees physically absent. SQLite foreign-key violations remain
empty and both source checkouts retain their original clean content.

The parallel scenario additionally asserts three Runs/Results/verifications,
one exact-target CAS winner, one `INTEGRATION_TARGET_MOVED` conflict, exact
mixed integrated/verified fan-in, and zero dispatches for the still-blocked
conflict consumer. This is a retained regression, not a new participant-
selection or finalizer feature.

### Results And Remaining Authority

Three consecutive packaged runs passed both tests with no skipped cases:

| Owned run suffix | Controlled loop | Parallel composition | Total |
| --- | ---: | ---: | ---: |
| `4GZM2x` | 23.185 s | 24.832 s | 48.198 s |
| `KQLF3F` | 23.822 s | 25.458 s | 49.456 s |
| `yEjdx3` | 22.618 s | 24.923 s | 47.726 s |

The final default-source regression also passed both tests in 56.562 seconds;
the incomplete-pin negative check, 382-file Markdown lint and whitespace gate
passed. All test-run roots were physically removed, the owned `runs` base was
empty, no `cw-product-*` container remained, and the downloaded/extracted
35 MB task root was removed after inspection. Before/after four-prefix scans
retained the same one pre-existing directory in `/private/tmp` and the same
17 in the user temporary root. The user's existing Central remained healthy
on its unchanged image and was never targeted.

Public issue review returned no open issues and public stable Latest remained
`v0.4.2`. This records technical completion of the gate-5 product execution and
gate-7 cleanup for rc.6, with the original harness failures disclosed above.
It is submitted for Owner review, not a fabricated Owner acceptance. QA-060
remains `ACTIVE`; the exact commit containing this test/evidence update must
pass all four main CI jobs, and Owner acceptance must precede stable source
admission or a new stable tag/Draft. No tag, Draft, published asset or Latest
designation was changed by this work.

### Owner Acceptance — 2026-09-05

After receiving the three-round packaged smoke results and the explicit
deterministic-model, API-driven and non-browser limitations above, the Owner
confirmed acceptance. This closes the Owner review portion of pre-tag gate 5;
it does not broaden the accepted physical-platform or provider claims.

The accepted evidence commit is
`b9119355718938729c217456d8c26f778f8fc690`. Exact-source main CI
[33952355193](https://github.com/chenrgSix/ConveneWire/actions/runs/33952355193)
passed Repository, Go, Desktop macOS and Desktop Windows. At confirmation,
local `main` and `origin/main` both resolved to that commit, the worktree was
clean and no `v0.5.0` Release existed.

This confirmation accepts the product-smoke evidence only. It does not by
itself authorize creating a stable tag/Draft, dispatching packaging or public
publication. QA-060 remains `ACTIVE`. When release execution is authorized,
select the then-current clean main commit and verify its exact-source CI,
refresh the pre-tag checks, and execute all protected/publication gates below.

## Protected Release Gates

After the pre-tag evidence passes:

1. Create a new annotated `v0.5.0` tag resolving to the admitted full commit
   and a new empty, non-prerelease Draft for that tag. Do not promote, retag or
   reuse the `v0.5.0-rc.5` Release or its assets.
2. Every source-consuming protected Release job must check out the one resolved
   full SHA and reconfirm that the tag still resolves to it before upload and
   verification.
3. All 10 protected Release executions must pass: Repository and Go gates, both
   Linux standalone CLI builds, native Apple-silicon macOS Desktop, native
   Windows packaging/lifecycle, the single Central source package, publication
   staging and post-download verification.
4. Native Windows must install current stable `v0.4.2`, upgrade in place to
   `v0.5.0`, preserve representative Owner state, verify protocol registration
   and installed/staging/ZIP executable identity, then uninstall without
   deleting Owner state.
5. The single Central source archive must expose its separately pinned,
   exhaustive internal checksum; all three bundled controller architectures
   must match the exact Release. One supported physical target must build and
   start it through `convenewirectl`, then report matching Server Release/source
   identity rather than readiness alone.
6. The closed set of exactly 12 assets must pass the tagged verifier before
   upload and after a fresh authenticated Draft download. A second clean,
   task-owned pre-publication directory must independently download and verify
   the same Draft assets.

## Publication Gates

Publication is authorized only after all pre-tag and protected gates pass:

1. Publish the Draft as non-draft and non-prerelease. It must become GitHub
   stable Latest without changing the tag or replacing any uploaded asset.
2. With GitHub credentials, netrc and user curl configuration absent, a fresh
   task-owned directory must download all 12 public assets and pass the tagged
   verifier. Public `/releases/latest` must resolve to `v0.5.0` with HTTP 200.
3. Retain the outer `SHA256SUMS` digest, total asset bytes, individual GitHub
   asset digests, publication timestamp and authenticated and anonymous
   verifier output.
4. Only after public verification, update README, release notes, website and
   version metadata from prerelease to stable. Static site, build and docs gates
   must pass, and public Pages bytes must match the exact deployed source.
5. Remove every task-owned checkout, download, database, cache, container and
   temporary root. Recheck the four historical temporary prefixes and held-
   deleted files physically rather than treating passing tests as cleanup
   evidence.

## Failure Policy

Any failed, canceled, timed-out, ambiguous or unverifiable gate authorizes no
tag movement, asset replacement, Draft publication, skipped assertion, Latest
change or completion claim. Diagnose the failure, record it, commit a bounded
repair with regression evidence, select a new exact source and rerun every gate
whose source or output may have changed.

Published and historical Release records are append-only evidence. A bad
candidate gets a new version identity; it is never repaired in place.

## Evidence Required For `DONE`

`QA-060` may become `DONE` only when one acceptance update retains:

- the admitted source SHA, annotated tag object and clean main/origin state;
- RC review decision and zero unresolved in-scope P0/P1 findings;
- exact-source main CI and all protected Release job identifiers and results;
- packaged product smoke, v0.4.2 backup, migration and isolated restore facts;
- native Windows previous/candidate versions and owner-state preservation;
- the Central source archive/pin, three helper identities and physical
  source-build runtime identity;
- all three authenticated/anonymous 12-asset verifier results, byte totals and
  checksum digests;
- non-prerelease publication metadata and public stable Latest resolution;
- exact-source README/site/Pages deployment and public-byte comparison; and
- before/after physical cleanup snapshots with zero owned residue.
