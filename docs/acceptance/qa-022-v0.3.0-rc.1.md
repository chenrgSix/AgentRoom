# QA-022 v0.3.0-rc.1 Release Candidate Acceptance

## Result

- Date: 2026-08-26
- Release source: tag `v0.3.0-rc.1`, commit
  `29d82cd290d4d50e0c47d1690332104a53a7a067`
- Result: **PASS**
- Published prerelease:
  <https://github.com/chenrgSix/AgentRoom/releases/tag/v0.3.0-rc.1>

This acceptance closes release task `QA-022`. The candidate packages the
version-aligned Server, Web, contracts, and Bridge changes for local Bridge
recovery and consent, bounded Runtime discovery, Codex Task Session guidance,
browser Mention retention, and the safe Agent Runtime policy projection.

Prerelease publication does not claim stable admission, signed or notarized
packages, automatic updates, internet-scale identity, high availability,
universal production readiness, the real macOS login-restart gate `BRG-013`,
or the two-physical-machine gate `QA-002`.

## Exact-Source Continuous Integration

Main CI run
[32956121778](https://github.com/chenrgSix/AgentRoom/actions/runs/32956121778)
completed successfully on the exact tagged commit. The Repository, Go, and
Desktop macOS jobs passed locked dependency installation, 7 schemas with 57
fixtures, production builds, 129 Server tests, 42 Web tests, 4 contract tests,
deterministic cross-process E2E, documentation lint, patch whitespace, Compose
validation, Go tests/vet, release-script checks, and Desktop test/vet/build.

## Local Release Preflight

The tagged source passed the same Node, Go, Desktop, contract, E2E, Compose,
release-script, and documentation gates locally. It additionally passed 17
Bridge UI tests, race-enabled Bridge tests, local native Desktop compilation,
and darwin/arm64 CLI and Desktop package checks with the embedded
`v0.3.0-rc.1` version. The credentialed Codex/Pi live E2E case remained
explicitly skipped, and an audit against the official npm registry reported
zero vulnerabilities.

One first test attempt ran Web, Go race, and Desktop compilation concurrently
and produced a Web Discussion timeout under local resource contention. The
complete isolated Node test rerun passed. The first E2E attempt could not write
the sandbox-blocked default Go caches; the same suite passed with isolated
temporary caches. The configured npm mirror did not implement the audit
endpoint, while the official npm registry audit completed successfully. These
environmental attempts did not require a source change and are not counted as
passing evidence.

## Release Workflow

Release Bridge run
[32956409046](https://github.com/chenrgSix/AgentRoom/actions/runs/32956409046)
validated the exact tag and empty draft, tested the tagged Bridge, built five
portable CLI archives and two native macOS Desktop archives, generated
checksums, added the three license assets, and verified the complete 11-asset
set before upload. It then downloaded the draft assets and repeated the tag's
committed verifier. Every job passed before the draft was published as a
prerelease at `2026-08-26T10:08:50Z`.

## Independent Public Asset Verification

A fresh directory at
`/private/tmp/agentroom-v030rc1-public-assets.eA3Jin` downloaded the public
prerelease and ran `bridge/scripts/verify-release-assets.sh` with
`RELEASE_TAG=v0.3.0-rc.1`. The verifier reported `Verified 11 release assets
for v0.3.0-rc.1`: five CLI archives, two Desktop ZIPs, `SHA256SUMS`, `LICENSE`,
`NOTICE`, and `COMMERCIAL-LICENSE.md`. It rechecked checksums, archive layout,
launchers, executable modes, architectures, embedded versions, GUI bundle
metadata, and license material.

## Remaining Gates

- `BRG-013` remains active until a real macOS logout/login or restart proves
  exactly one desktop instance without credentials in the LaunchAgent.
- `QA-002` remains ready until two physical machines produce reviewed online
  and offline/reconnect Codex Runs.
- Packages remain unsigned, unnotarized, and manual-update-only.
- `v0.2.0` remains the stable Latest release while this version is evaluated as
  a prerelease candidate.
