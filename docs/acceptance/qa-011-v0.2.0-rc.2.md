# QA-011 v0.2.0-rc.2 Release Acceptance

## Result

- Date: 2026-08-24
- Release source: tag `v0.2.0-rc.2`, commit
  `4dfd5e1b2a8f3f0a294a0e744702ee9b2955eb2a`
- Result: **PASS**
- Published candidate:
  <https://github.com/chenrgSix/AgentRoom/releases/tag/v0.2.0-rc.2>

This acceptance closes the rc.2 publication scope. It does not close the real
macOS login-restart gate `BRG-013` or the two-physical-machine managed Agent
gate `QA-002`.

## Continuous Integration

Main CI run
[32652918646](https://github.com/chenrgSix/AgentRoom/actions/runs/32652918646)
passed on the exact tagged commit. Its Repository, Go, and Desktop macOS jobs
validated schemas, Node builds/tests, deterministic E2E, maintained Markdown,
patch whitespace, central Compose configuration, ordinary Go tests/vet, release
scripts, and Desktop-tagged tests/vet/build.

Local release preflight passed 89 Server tests, 8 Web tests, 4 contract tests,
7 schemas with 30 fixtures, deterministic E2E, and ordinary plus Desktop-tagged
Bridge tests/vet. A managed Bridge E2E timed out only while competing with four
parallel Go jobs; its isolated rerun and the serial GitHub CI run both passed.

## Release Workflow

Release run
[32653022605](https://github.com/chenrgSix/AgentRoom/actions/runs/32653022605)
validated an empty draft, tested the tagged Bridge, built five portable CLI
archives and two native macOS GUI archives, generated checksums, added license
assets, verified the complete set before upload, downloaded the draft assets,
and repeated the verifier. Every job passed before the draft was published as a
prerelease.

## Independent Asset Verification

A new empty temporary directory downloaded the public release and ran
`bridge/scripts/verify-release-assets.sh` with `RELEASE_TAG=v0.2.0-rc.2`. The
verifier reported 11 valid assets: five CLI archives, two desktop ZIPs,
`SHA256SUMS`, `LICENSE`, `NOTICE`, and `COMMERCIAL-LICENSE.md`. It rechecked
checksums, archive layout, launchers, executable modes, architectures, embedded
versions, GUI bundle metadata, and license material.

## Runtime Evidence and Remaining Gates

Development validation completed an isolated parallel Codex and Pi Wave with
Pi finalization. Ordinary CI intentionally skips credentialed local Runtime
execution. The candidate remains unsigned and manual-update-only; `BRG-013` and
`QA-002` remain separate physical-environment gates.
