# QA-015 v0.2.0-rc.4 Release Acceptance

## Result

- Date: 2026-08-24
- Release source: tag `v0.2.0-rc.4`, commit
  `ecb8593a0905d01c8139da4988424bf8ea13f1e0`
- Result: **PASS**
- Published candidate:
  <https://github.com/chenrgSix/AgentRoom/releases/tag/v0.2.0-rc.4>

This acceptance closes the rc.4 publication scope and `QA-015`. It does not
close the real macOS login-restart gate `BRG-013`, the two-physical-machine
managed Agent gate `QA-002`, or credentialed Codex and Pi execution on the
tagged binaries.

## Delivered Scope

The release source completes event-driven selected-Room reconciliation,
idempotent recoverable Message submission, recoverable Team/Room/Agent
lifecycle controls, and Bridge Runtime draft preflight. It also includes the
preceding Room composer docking, visible Team creation, per-Room participant
management, and independent per-Agent Bridge configuration changes. The
authoritative task evidence is recorded by `WEB-026` through `WEB-028`,
`ROOM-005`, `REG-004`, `BRG-021`, and `BRG-022` in `docs/TASKS.md`.

## Continuous Integration

Main CI run
[32697973957](https://github.com/chenrgSix/AgentRoom/actions/runs/32697973957)
passed on the exact tagged commit. Its Repository, Go, and Desktop macOS jobs
validated 7 schemas and 30 fixtures, Node builds, 95 Server tests, 12 Web tests,
4 contract tests, deterministic cross-process E2E, maintained Markdown, patch
whitespace, central Compose configuration, ordinary Go tests and vet, release
scripts, and Desktop-tagged tests, vet, and native compilation.

## Local Release Preflight

The final source passed the same contract, Node, E2E, documentation, Compose,
release-script, ordinary Go, and Desktop-tagged gates before push. Focused
regressions additionally prove monotonic Team-change wakeup and restart reset,
stable-ID archive/restore and Agent disable/enable fences, same-ID Message retry
without duplicate work, docked composer layout, and unsaved Runtime preflight
without configuration persistence or Bridge restart.

## Release Workflow

Release run
[32698124280](https://github.com/chenrgSix/AgentRoom/actions/runs/32698124280)
validated the requested tag and an empty draft, tested the tagged Bridge, built
five portable CLI archives and two native macOS GUI archives, generated
checksums, added license assets, and verified the complete 11-asset set before
upload. It then downloaded the draft assets and repeated the committed verifier.
Every job passed before the draft was published as a prerelease.

## Independent Asset Verification

After publication, a new empty directory at
`/private/tmp/agentroom-rc4-public-assets-20260824` downloaded the public
release and ran `bridge/scripts/verify-release-assets.sh` with
`RELEASE_TAG=v0.2.0-rc.4`. The verifier reported 11 valid assets: five CLI
archives, two desktop ZIPs, `SHA256SUMS`, `LICENSE`, `NOTICE`, and
`COMMERCIAL-LICENSE.md`. It rechecked checksums, archive layout, launchers,
executable modes, architectures, embedded versions, GUI bundle metadata, and
license material.

## Remaining Gates

The candidate remains unsigned and manual-update-only. `BRG-013` and `QA-002`
remain separate physical-environment gates, and ordinary CI continues to skip
credentialed local Runtime execution.
