{$AGENT_ROOM_DOMAIN} {
	encode zstd gzip

	@metrics path /api/metrics
	respond @metrics 404

	header {
		-Server
		Referrer-Policy "same-origin"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
	}

	reverse_proxy agentroom:3000
}
