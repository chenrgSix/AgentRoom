{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agentroom.dev/schemas/common/identifiers.schema.json",
  "title": "Agent Room Opaque Identifier",
  "description": "Opaque identifier with a lowercase type prefix and non-semantic suffix.",
  "$ref": "#/$defs/opaqueId",
  "$defs": {
    "opaqueId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*_[A-Za-z0-9_-]{8,128}$"
    },
    "teamId": {
      "type": "string",
      "pattern": "^team_[A-Za-z0-9_-]{8,128}$"
    },
    "roomId": {
      "type": "string",
      "pattern": "^room_[A-Za-z0-9_-]{8,128}$"
    },
    "memberId": {
      "type": "string",
      "pattern": "^member_[A-Za-z0-9_-]{8,128}$"
    },
    "deviceId": {
      "type": "string",
      "pattern": "^device_[A-Za-z0-9_-]{8,128}$"
    },
    "agentId": {
      "type": "string",
      "pattern": "^agent_[A-Za-z0-9_-]{8,128}$"
    },
    "agentProvisionRequestId": {
      "type": "string",
      "pattern": "^agentprov_[A-Za-z0-9_-]{8,128}$"
    },
    "messageId": {
      "type": "string",
      "pattern": "^msg_[A-Za-z0-9_-]{8,128}$"
    },
    "runId": {
      "type": "string",
      "pattern": "^run_[A-Za-z0-9_-]{8,128}$"
    },
    "taskId": {
      "type": "string",
      "pattern": "^task_[A-Za-z0-9_-]{8,128}$"
    },
    "artifactId": {
      "type": "string",
      "pattern": "^artifact_[A-Za-z0-9_-]{8,128}$"
    },
    "contentId": {
      "type": "string",
      "pattern": "^content_[A-Za-z0-9_-]{8,128}$"
    },
    "traceId": {
      "type": "string",
      "pattern": "^trace_[A-Za-z0-9_-]{8,128}$"
    },
    "deliveryAttemptId": {
      "type": "string",
      "pattern": "^delivery_[A-Za-z0-9_-]{8,128}$"
    },
    "handoffId": {
      "type": "string",
      "pattern": "^handoff_[A-Za-z0-9_-]{8,128}$"
    },
    "sessionId": {
      "type": "string",
      "pattern": "^session_[A-Za-z0-9_-]{8,128}$"
    }
  }
}
